<div class="container">
	  <audio autoplay hidden="true">
      <source src="track.mp3" type="audio/mpeg">
      <embed height="0" width="0" src="track.mp3">
   </audio>

  <table class="racer_table">
    <% @game.players.each do |player|  %>
      <tr class="strip" id="<%= player.id %>">
        <td class="active"></td>
      </tr>
    <% end %>
  </table>
</div> 

<script>
  var gameId = <%= @game.id %>
</script>


